# Use fdisk -l to identify device name
DISK=sda
# Check the region and country list
# https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
REGION=America
COUNTRY=Costa_Rica
#
COMPUTER=Computer-Name
USERNAME=User-Name
# CPU: 'amd' or 'intel'
CPU=amd
# GPU: 'mesa' (amd and intel),
#      'nvidia',
#      'both' (mesa and nvidia),
#      'virtual' (virtual machine)
GPU=virtual
